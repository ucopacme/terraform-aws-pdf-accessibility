# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
.terraform.lock.hcl

# Lambda zip artifacts generated by archive_file
**/.lambda_zips/

# Old pre-built zip artifacts (no longer used)
**/builds/

# Large binary artifacts
deployment-example/lambda/zip/*.zip
deployment-example/lambda/pdf-merger/*.jar
